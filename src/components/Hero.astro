---

---

<section id="about" class="hero">
    <div class="content">
        <div class="profile-container">
            <img
                src="https://raw.githubusercontent.com/bmteo/bmteo.github.io/refs/heads/master/public/img/portfolio/fullsize/profile.jpg"
                alt="Benjamin Teo"
                class="profile-img"
            />
        </div>
        <span class="greeting">Heya, I'm</span>
        <h1>Benjamin Teo</h1>
        <p class="subtitle">Senior Software Engineer</p>
        <p class="description">
            Specializing in large-scale distributed systems and technical
            leadership. Leading cross-functional, full-stack projects features
            for multi-tenant platforms across Amazon's lines of business.
        </p>
        <div class="cta-group">
            <a href="#contact" class="btn btn-primary">Let's Connect</a>
            <a
                href="/Files/TechnicalRESUME.pdf"
                target="_blank"
                class="btn btn-secondary">Resume</a
            >
        </div>
    </div>
</section>

<style>
    .hero {
        min-height: 85vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 4rem 1.5rem;
        max-width: var(--spacing-container);
        margin: 0 auto;
        text-align: center;
    }

    .content {
        max-width: 800px;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .profile-container {
        margin-bottom: 2.5rem;
        position: relative;
        display: inline-block;
    }

    .profile-img {
        width: 150px;
        height: 150px;
        object-fit: cover;
        border-radius: 50%;
        border: 4px solid var(--color-text-primary);
        box-shadow: 10px 10px 0 var(--color-accent);
        background-color: var(--color-surface); /* Fallback */
    }

    .greeting {
        color: var(--color-text-primary);
        font-size: 2rem;
        margin-bottom: 0.5rem;
        display: block;
        font-family: var(--font-heading);
        font-style: italic;
    }

    /* Override h1 to match the "Shailesh Acharya" style if needed, 
       but keeping user's Heading font for consistency */
    h1 {
        font-size: 4.5rem;
        line-height: 1.1;
        margin-bottom: 0.5rem;
        color: var(--color-accent);
    }

    .subtitle {
        font-size: 1.2rem;
        color: var(--color-text-primary);
        margin-bottom: 2rem;
        font-family: var(--font-body);
        font-weight: 300;
        opacity: 0.9;
    }

    .description {
        font-size: 1.1rem;
        color: var(--color-text-secondary);
        max-width: 650px;
        margin-bottom: 2.5rem;
        line-height: 1.6;
    }

    .cta-group {
        display: flex;
        gap: 1.5rem;
        justify-content: center;
    }

    .btn {
        padding: 1rem 2rem;
        font-family: var(--font-heading);
        font-size: 1.2rem;
        border-radius: 8px;
        transition: all 0.2s ease;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .btn-primary {
        background: var(--color-accent);
        color: var(--color-bg);
        border: 2px solid var(--color-accent);
    }

    .btn-primary:hover {
        background: transparent;
        color: var(--color-accent);
        transform: translateY(-2px);
    }

    .btn-secondary {
        background: transparent;
        color: var(--color-text-primary);
        border: 2px solid var(--color-text-primary);
    }

    .btn-secondary:hover {
        background: rgba(255, 255, 255, 0.1);
        transform: translateY(-2px);
    }

    @media (max-width: 768px) {
        .hero {
            padding: 2rem 1rem;
        }

        h1 {
            font-size: 2.5rem; /* Significantly reduced from 4.5rem */
        }

        .greeting {
            font-size: 1.5rem;
        }

        .subtitle {
            font-size: 1rem;
        }

        .profile-img {
            width: 130px;
            height: 130px;
        }

        .cta-group {
            flex-direction: column;
            width: 100%;
            gap: 1rem;
        }

        .btn {
            width: 100%;
        }
    }
</style>
